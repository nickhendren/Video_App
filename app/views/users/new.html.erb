<!DOCTYPE html>
<html lang="en">
<div class="row">
  <div class="col-lg-offset-3 col-lg-6 col-md-offset-3 col-md-6 col-sm-offset-3 col-sm-6">
    <div class="thumbnail">
      <div class="caption">
  <% if current_user %>
    <br><br><br><br><br><br><br><br><br><br>
    <h2>You already have an Account</h2>
    <h2><a href="/posts">Click here to view the posts</a></h2>
  <% else %>
    <h1>Signing Up is Easier than ever!</h1>
    <p>Enter your information below</p>
    <%= bootstrap_form_for(@user, layout: :horizontal, label_col: "col-sm-4", control_col: "col-sm-4") do |f| %>
      <%= f.text_field :first_name %>
      <%= f.text_field :last_name %>
      <%= f.email_field :email %>
      <%= f.text_field :user_name %>
      <%= f.password_field :password %>
      <%= f.form_group do %>
        <%= f.submit "Create New Account" %>
      <% end %>
    <% end %>
  <% end %>
</div>
</div>
</div>
</div>
</html>
